---
import "../../styles/global.css";
import { Image } from "astro:assets";
import avatar from "../../assets/avatar_benji.jpg";
---

<article class="container">
  <Image src={avatar} alt="Une photo de Benjamin" class="avatar" />
  <div>
    <h2>Bienvenue sur mon site !</h2>
    <p class="description">
      Je m'appelle Benjamin, je suis <strong>Pianiste</strong>, <strong
        >Compositeur</strong
      > et <strong>professeur</strong>.<br /><br /> N'hésites pas à visiter <strong
        >les différents cours</strong
      > pour avoir plus d'informations !<br /><br />
      <a href="/qui"
        >Si tu veux en savoir plus sur mon parcours,
        <span class="underline">&nbsp;c'est&nbsp;par&nbsp;ici&nbsp;!&nbsp;</span
        ></a
      >
    </p>
  </div>
</article>

<style>
  article {
    color: var(--text-color);
    height: var(--article-height-mobile);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    font-size: 1.2rem;
    padding: 1rem;
  }
  h2 {
    font-size: 2rem;
    margin-bottom: 1.2rem;
  }
  .description {
    margin-bottom: 1.2rem;
    text-align: justify;
    text-wrap: wrap;
  }
  .avatar {
    border-radius: 50%;
    width: 200px;
    height: 200px;
    margin-bottom: 2rem;
  }
  a {
    cursor: pointer;
    color: var(--text-color);
    text-decoration: none;
  }
  .underline {
    font-weight: bold;
    border-bottom: 4px solid var(--primary-400);
  }

  @media (width >= 1024px) {
    article {
      flex-direction: row;
      justify-content: space-between;
    }
    .avatar {
      width: 300px;
      height: 300px;
      margin-right: 4rem;
    }
  }
  @media (width >= 1024px) {
    article {
      height: var(--article-height-desktop);
    }
  }
  @media (width >= 1200px) {
    article {
      font-size: 1.4rem;
    }
  }
</style>
