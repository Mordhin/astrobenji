---
import "../../styles/global.css";
import { Image } from "astro:assets";
import contact from "../../assets/contact.svg";
---

<article class="container">
  <Image src={contact} alt="" class="contact-img" />
  <div>
    <h2>Tarifs & Contact</h2>
    <p class="description">
      <a href=""><span class="underline">Clique ici</span></a>, tu trouveras
      toutes les informations concernant les <strong>tarifs</strong> de mes cours,
      ainsi que les <strong>moyens de me contacter</strong> !
    </p>
  </div>
</article>

<style>
  article {
    color: var(--text-color);
    height: var(--article-height-mobile);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    font-size: 1.2rem;
    padding: 1rem;
  }
  h2 {
    font-size: 2rem;
    margin-bottom: 1.2rem;
  }
  .description {
    margin-bottom: 1.2rem;
    text-align: justify;
    text-wrap: wrap;
  }
  .contact-img {
    width: 200px;
    height: 200px;
    margin-bottom: 2rem;
  }
  a {
    cursor: pointer;
    color: var(--text-color);
    text-decoration: none;
  }
  .underline {
    font-weight: bold;
    border-bottom: 4px solid var(--primary-400);
  }

  @media (width >= 1024px) {
    article {
      flex-direction: row;
      justify-content: space-between;
    }
    .contact-img {
      width: 300px;
      height: 300px;
      margin-right: 4rem;
    }
  }
  @media (width >= 1024px) {
    article {
      height: var(--article-height-desktop);
    }
  }
  @media (width >= 1200px) {
    article {
      font-size: 1.4rem;
    }
  }
</style>
