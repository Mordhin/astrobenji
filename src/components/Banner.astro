---
import "../styles/global.css";
import { Icon } from "astro-icon";
import Button from "./Button.astro";
---

<header id="banner">
  <h1 class="title">
    Hello !<br />
    Je suis <span class="underline">Benjamin</span>,<br />
    Pianiste et Professeur<span class="dot">.</span>
  </h1>
  <Button href="/contact">Je réserve un cours</Button>
  <div class="expand">
    <Icon name="chevron-compact-down" />
  </div>
</header>

<style>
  h1 {
    font-size: 2rem;
  }
  header {
    height: 100vh;
    background: linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.6)),
      url("../assets/synth-banner.jpg");
    background-position: center;
    background-size: cover;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: left;
    color: var(--text-color-reversed);
    font-weight: bold;
    padding: 1rem;
  }
  .title {
    margin-bottom: 1rem;
  }
  .underline {
    border-bottom: 4px solid var(--primary-400);
  }
  .dot {
    color: var(--primary-400);
  }
  .expand svg {
    width: 2rem;
    height: min-content;
    fill: white;
    margin: 1rem auto;
    position: absolute;
    left: calc(50% - 1rem);
    bottom: 0;
    cursor: pointer;
    transition: bottom 300ms ease;
  }
  .expand svg:hover,
  .expand svg:active {
    transition: bottom 300ms ease;
    bottom: -0.5rem;
  }
  @media (width >= 480px) {
    header {
      padding: 2rem;
    }
    h1 {
      font-size: 2.5rem;
    }
  }
  @media (width >= 724px) {
    header {
      padding: 3rem;
    }
    h1 {
      font-size: 3rem;
    }
  }
  @media (width >= 1024px) {
    header {
      padding: 4rem;
    }
    h1 {
      font-size: 4rem;
    }
  }
</style>
